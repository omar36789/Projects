name: capstone_app_new
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  table_calendar: ^3.0.6
  firebase_core: ^2.4.1
  firebase_auth: ^4.1.1
  cloud_firestore: ^4.17.5
  provider: ^6.0.5
  uuid: ^3.0.6
  url_launcher: ^6.1.10
  image_picker: ^0.8.5+3
  firebase_storage: ^11.0.6
  file_picker: ^4.2.6
  logger: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
